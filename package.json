{
  "name": "nexodirecto",
  "version": "0.1.0",
  "private": true,
  "description": "NexoDirecto — valuacion autogestionada + marketplace (NexoNegocios)",
  "workspaces": [
    "engine",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspace engine",
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web"
  },
  "license": "UNLICENSED"
}
